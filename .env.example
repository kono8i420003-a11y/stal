# TELEGRAM CONFIGURATION
# Token received from @BotFather in Telegram.
TELEGRAM_BOT_TOKEN=""
# Chat ID (or Group Chat ID) where notifications should be delivered. Can be retrieved from @userinfobot or @GetMyChatID_Bot.
TELEGRAM_CHAT_ID=""

# SUPABASE CONFIGURATION
# Project URL from Supabase project settings (Project Settings -> API).
SUPABASE_URL=""
# Service role key (server-side only, never expose to the client) from Project Settings -> API.
SUPABASE_SERVICE_ROLE_KEY=""

# ADMIN PANEL
# Password required to open the "ЦУП «СТАЛЬ»" leads dashboard (sent as X-Admin-Secret header).
ADMIN_PANEL_SECRET=""
